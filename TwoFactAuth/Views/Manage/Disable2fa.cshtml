@{
    ViewData["Title"] = "Deshabilitar la autenticación de dos factores (2FA)";
    ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Esta acción solo desactiva 2FA.</strong>
    </p>
    <p>
        La desactivación de 2FA no cambia las claves utilizadas en las aplicaciones de autenticación. Si desea cambiar la clave
        utilizado en una aplicación de autenticación, debe <a asp-action="ResetAuthenticatorWarning">reinicia tus claves del autenticador.
</a>
    </p>
</div>

<div>
    <form asp-action="Disable2fa" method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Deshabilitar 2FA</button>
    </form>
</div>
